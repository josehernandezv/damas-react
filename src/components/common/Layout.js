import styled from 'styled-components';
import imgSrc from 'assets/wood-background.jpg';

const CenteredContainer = styled.div`
    background-image: url(${imgSrc});
`;

export default CenteredContainer;
